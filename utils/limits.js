//This file is to store data related to a data limits across the app

const PIN_POST_DAYS = {
  DEFAULT: 7,
  MAX: 30,
  MIN: 1,
}

const PERFECT_SCORE = 5

module.exports = {
  PERFECT_SCORE,
  PIN_POST_DAYS,
}
